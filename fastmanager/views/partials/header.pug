header#header
    nav.top-nav
        div.container
            div.nav-wrapper
                a.page-title FastManager

    div.container
        a(href="#" data-activates="nav-mobile" class="button-collapse top-nav full hide-on-large-only")
            i.material-icons menu

    ul#nav-mobile.side-nav.fixed.full
        li
            a(href="/") 홈
        if (!user)
            li
                a(href="/signup/") 회원가입
            li
                a(href="/login/") 로그인
        else
            li
                a(href="/logout/") 로그아웃
        li
            a(href="/lectures/") 모든 강의 살펴보기
        if (user)
            li
                a(href="/" + type + "s/lectures/") 내 강의 살펴보기
